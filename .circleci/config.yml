version: 2.1

orbs:
  ci: bigcommerce/internal@volatile
  php: bigcommerce/internal-php@volatile

jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - ci/clone-repo:
          repo: ndintenfass/scratch

version: 2.0

jobs:
  build:
    docker:
      - image: docker:17.03.2-ce
    working_directory: ~/repo
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build app
          command: |
            docker run -v $PWD/repo:/app ubuntu ls -la
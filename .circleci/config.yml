version: 2.0

jobs:
  build:
    docker:
      - image: docker:17.10.0-ce-git
    working_directory: ~/repo
    steps:
      - setup_remote_docker
      - run:
          name: update_stack2
          command: |
            curl -vvv -s -X PUT -d "`echo foo`" -H "X-Aglet-Token: \"whatebver\"" -k "https://httpbin.org/put"



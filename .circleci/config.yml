version: 2
executors:
  hugo:
    docker:
      - image: cibuilds/hugo:latest
orbs:
  utils: ndintenfass/build-utils@volatile
  # orb-docs:
  #   orbs:
  #     utils: ndintenfass/built-utils@volatile
  #   executors:
  #     hugo:
  #       docker:
  #         - image: cibuilds/hugo:latest
  #   commands:
  #     list-orbs:
  #       steps:
  #         - utils/install-circleci-cli
  #         - run: circleci orb list

jobs:
  build:
    executor: hugo
    steps:
      - utils/install-circleci-cli
      - run: echo after




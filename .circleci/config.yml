version: 2.1

jobs:
  one:
    docker:
      - image: circleci/node:latest
    steps:
      - run: echo succeed
  two:
    docker:
      - image: circleci/node:latest
    steps:
      - run: echo fail && exit 1
  three:
    docker:
      - image: circleci/node:latest
    steps:
      - run: echo succeed

workflows:
  main:
    jobs:
      - one
      - two
      - three:
          requires:
            - two
version: 2.0

workflows:
  version: 2
  whatever:
    jobs:
      - foobuildme


jobs:
  foobuildme:
    docker:
      - image: docker:17.10.0-ce-git
    working_directory: ~/repo
    steps:
      - run: echo whatever


